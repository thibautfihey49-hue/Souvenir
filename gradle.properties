# Activer AndroidX (obligatoire)
android.useAndroidX=true

# Ne pas utiliser les anciennes bibliothèques de support
android.enableJetifier=false

# Encodage
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Non-parallèle pour éviter les erreurs en CI
org.gradle.parallel=false
